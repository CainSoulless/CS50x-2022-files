#include <stdio.h>
#include <cs50.h>
#include <ctype.h>
#include <string.h>

int main(void)
{
    char points[] = {1,2,3,4,5,6};
    int sum;

    // for (int i = 0; i < strlen(points); i++)
    // {

    // }

    sum = points[0] + points[5];
    printf("%d\n", sum);

}